{
  "name": "browser-youtube-play-all",
  "version": "0.4.3",
  "private": true,
  "description": "Adds \"Play All\" buttons for Videos, Shorts, and Streams on YouTube, with sorting by Latest, Popular, or Oldest.",
  "type": "module",
  "scripts": {
    "build": "conc \"bun:build:*\"",
    "build:chrome": "wxt build -b chrome",
    "build:firefox": "wxt build -b firefox",
    "check": "bun ci && vp check",
    "dev": "bun run dev:chrome",
    "dev:chrome": "wxt -b chrome",
    "dev:firefox": "wxt -b firefox",
    "publish": "bun run zip && wxt submit",
    "publish-init": "wxt submit init",
    "publish-refresh-token-init": "bunx chrome-webstore-upload-keys",
    "setup": "bun i && wxt prepare && bunx playwright install chromium firefox",
    "test": "bun run build && bunx playwright test tests/e2e",
    "test-specs": "bun run build && bunx playwright test tests/specs",
    "test-ui-mode": "bun run build && bunx playwright test --ui",
    "zip": "conc \"bun:zip:*\"",
    "zip:chrome": "wxt zip -b chrome",
    "zip:firefox": "wxt zip -b firefox"
  },
  "dependencies": {
    "eventemitter3": "^5.0.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.2.9",
    "concurrently": "^10.0.5",
    "playwright-webextext": "^0.0.5",
    "typescript": "^7.0.2",
    "vite-plus": "^0.3.1",
    "wxt": "0.21.4"
  }
}
